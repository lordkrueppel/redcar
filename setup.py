from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A test repo for the redcar project',
    author='Rico',
    license='BSD-3',
)
